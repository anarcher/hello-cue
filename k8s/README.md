
1. `cue import `
2. `cue eval` `cue export`
3. `cue export | yq r -P -`
4. `mkdir alpha`
5. `cue export`
    ```$ cue export
    config.namespace: cannot convert incomplete value "string" to JSON
    ```
